# Git / IDE
.git
.gitignore
.cursor
.vscode
mcps

# Tests and caches
tests
__pycache__
*.py[cod]
.pytest_cache
.mypy_cache
.ruff_cache
htmlcov
.coverage

# Docs (not needed in image)
*.md
LICENSE

# .env is included in the image when present (production client builds). Never commit real .env to git.
# Service account JSON stays out of the image unless you intentionally add it (not recommended).
secrets

# Docker / compose (optional; avoids recursive context noise)
Dockerfile*
docker-compose*.yml
.dockerignore

# Misc
*.log
.DS_Store
Thumbs.db
